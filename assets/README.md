# assets 目录说明

统一使用本目录素材，便于后续替换。

- `icons/`：功能图标、导航图标、状态图标（SVG）
- `banners/`：顶部/中部横幅背景图（SVG）
- `bg/`：卡片背景图（SVG）

## 替换规则
1. 直接用同名文件覆盖即可。
2. 建议保持原尺寸比例（横幅约 750 宽、图标 64 画布）。
3. 优先使用 SVG；如替换 PNG，请保持同名并更新 `index.html` 路径。
